#!/bin/bash

require 'squire'

while [[ -n "$1" ]]; do
  case "$1" in
    install )
      shift
      break
      ;;
    uninstall )
      shift
      break
      ;;
    * )
      echo 'You can kiss my ass...'
      exit 1
      ;;
  esac
done
